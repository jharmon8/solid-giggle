import java.awt.*;

public class BasicBullet extends Projectile {
    BasicBullet(double px, double py, double vx, double vy, Color color, Shooter parent) {
        super(px, py, vx, vy, color, parent);
    }
}
